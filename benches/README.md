# Benchmarks

Crate-local Criterion benchmarks live next to the code they measure. Cross-engine and
dataset benchmarks will live here once availability/search milestones exist.

Current benchmark:

```bash
cargo bench -p veyra-runtime --bench snapshot
```
