name: Apply V1 solver pricing patch

on:
  push:
    branches:
      - agent/v1-production-completion
    paths:
      - .github/workflows/apply-v1-solver-pricing.yml
  workflow_dispatch:

permissions:
  contents: write

jobs:
  apply-and-verify:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v6
        with:
          ref: agent/v1-production-completion
      - name: Install pinned Rust
        run: rustup toolchain install 1.97.1 --profile minimal --component rustfmt --component clippy
      - name: Apply implementation patch
        run: git apply --check tools/patches/v1-solver-pricing.patch && git apply tools/patches/v1-solver-pricing.patch
      - name: Format workspace
        run: cargo fmt --all
      - name: Verify solver clippy
        run: cargo clippy -p veyra-solver --all-targets --all-features --locked -- -D warnings
      - name: Verify solver tests
        run: cargo test -p veyra-solver --all-features --locked
      - name: Commit verified implementation
        shell: bash
        run: |
          git rm tools/patches/v1-solver-pricing.patch .github/workflows/apply-v1-solver-pricing.yml
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add crates/veyra-solver/src/lib.rs
          git commit -m "feat: price solver allocations from actual occupancy"
          git push origin HEAD:agent/v1-production-completion
