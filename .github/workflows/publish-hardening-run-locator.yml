name: Publish hardening run locator

on:
  push:
    branches:
      - agent/v1-production-completion
    paths:
      - .github/workflows/publish-hardening-run-locator.yml

permissions:
  actions: read
  contents: write

jobs:
  locate:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v6
        with:
          ref: agent/v1-production-completion
      - name: Record latest hardening run
        env:
          GH_TOKEN: ${{ github.token }}
        shell: bash
        run: |
          gh api \
            "repos/$GITHUB_REPOSITORY/actions/workflows/coverage-portability-hardening.yml/runs?branch=agent%2Fv1-production-completion&per_page=1" \
            --jq '.workflow_runs[0] | [.id, .status, (.conclusion // "pending"), .head_sha] | @tsv' \
            > docs/.coverage-hardening-run
      - name: Commit locator and remove helper
        shell: bash
        run: |
          git rm .github/workflows/publish-hardening-run-locator.yml
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add docs/.coverage-hardening-run
          git commit -m "ci: record coverage hardening run"
          git push origin HEAD:agent/v1-production-completion
