diff --git a/crates/veyra-availability/src/lib.rs b/crates/veyra-availability/src/lib.rs
index f132fd0..f3fb7b2 100644
--- a/crates/veyra-availability/src/lib.rs
+++ b/crates/veyra-availability/src/lib.rs
@@ -22,7 +22,7 @@ impl DenseRoomSet {
         if room_id >= self.room_count {
             return false;
         }
-        let word = usize::try_from(room_id / WORD_BITS).unwrap_or(usize::MAX);
+        let word = (room_id / WORD_BITS) as usize;
         let bit = room_id % WORD_BITS;
         self.words
             .get(word)
@@ -42,11 +42,44 @@ impl DenseRoomSet {
         self.words.iter().all(|word| *word == 0)
     }
 
+    #[must_use]
+    pub fn iter(&self) -> DenseRoomIter<'_> {
+        DenseRoomIter {
+            words: &self.words,
+            word_index: 0,
+            pending: 0,
+        }
+    }
+
     #[must_use]
     pub fn room_ids(&self) -> Vec<u32> {
-        (0..self.room_count)
-            .filter(|id| self.contains(*id))
-            .collect()
+        self.iter().collect()
+    }
+}
+
+/// Allocation-free iterator over set room IDs in deterministic ascending order.
+#[derive(Clone, Debug)]
+pub struct DenseRoomIter<'a> {
+    words: &'a [u64],
+    word_index: u32,
+    pending: u64,
+}
+
+impl Iterator for DenseRoomIter<'_> {
+    type Item = u32;
+
+    fn next(&mut self) -> Option<Self::Item> {
+        loop {
+            if self.pending != 0 {
+                let bit = self.pending.trailing_zeros();
+                self.pending &= self.pending - 1;
+                let room_id = (self.word_index - 1) * WORD_BITS + bit;
+                return Some(room_id);
+            }
+
+            self.pending = *self.words.get(self.word_index as usize)?;
+            self.word_index += 1;
+        }
     }
 }
 
@@ -68,16 +101,10 @@ impl AvailabilityIndex {
         if room_count == 0 || room_count > MAX_ROOM_IDS {
             return Err(AvailabilityError::InvalidRoomCount(room_count));
         }
-        let words_per_day_u32 = room_count
-            .checked_add(WORD_BITS - 1)
-            .ok_or(AvailabilityError::SizeOverflow)?
-            / WORD_BITS;
-        let words_per_day =
-            usize::try_from(words_per_day_u32).map_err(|_| AvailabilityError::SizeOverflow)?;
-        let days = usize::try_from(day_count).map_err(|_| AvailabilityError::SizeOverflow)?;
-        let total_words = words_per_day
-            .checked_mul(days)
-            .ok_or(AvailabilityError::SizeOverflow)?;
+        // Public bounds keep all arithmetic below 12 million words, including on 32-bit usize.
+        let words_per_day = ((room_count + WORD_BITS - 1) / WORD_BITS) as usize;
+        let days = day_count as usize;
+        let total_words = words_per_day * days;
         Ok(Self {
             start_day,
             day_count,
@@ -97,13 +124,9 @@ impl AvailabilityIndex {
         if room_id >= self.room_count {
             return Err(AvailabilityError::RoomOutOfRange(room_id));
         }
-        let word_in_day =
-            usize::try_from(room_id / WORD_BITS).map_err(|_| AvailabilityError::SizeOverflow)?;
+        let word_in_day = (room_id / WORD_BITS) as usize;
         let bit = room_id % WORD_BITS;
-        let index = day_offset
-            .checked_mul(self.words_per_day)
-            .and_then(|base| base.checked_add(word_in_day))
-            .ok_or(AvailabilityError::SizeOverflow)?;
+        let index = day_offset * self.words_per_day + word_in_day;
         let word = self
             .words
             .get_mut(index)
@@ -144,7 +167,6 @@ impl AvailabilityIndex {
         }
         Ok(DenseRoomSet {
             words: result,
-            room_count: self.room_count,
         })
     }
 
@@ -185,12 +207,9 @@ impl AvailabilityIndex {
     }
 
     fn available_on_day(&self, day: u32, room_id: u32) -> Result<bool, AvailabilityError> {
-        if room_id >= self.room_count {
-            return Err(AvailabilityError::RoomOutOfRange(room_id));
-        }
+        // Callers iterate only over `0..room_count`; the public mutation API enforces the same bound.
         let slice = self.day_slice(day)?;
-        let word =
-            usize::try_from(room_id / WORD_BITS).map_err(|_| AvailabilityError::SizeOverflow)?;
+        let word = (room_id / WORD_BITS) as usize;
         let bit = room_id % WORD_BITS;
         Ok(slice
             .get(word)
@@ -199,12 +218,8 @@ impl AvailabilityIndex {
 
     fn day_slice(&self, day: u32) -> Result<&[u64], AvailabilityError> {
         let offset = self.day_offset(day)?;
-        let start = offset
-            .checked_mul(self.words_per_day)
-            .ok_or(AvailabilityError::SizeOverflow)?;
-        let end = start
-            .checked_add(self.words_per_day)
-            .ok_or(AvailabilityError::SizeOverflow)?;
+        let start = offset * self.words_per_day;
+        let end = start + self.words_per_day;
         self.words
             .get(start..end)
             .ok_or(AvailabilityError::InternalBounds)
@@ -217,7 +232,7 @@ impl AvailabilityIndex {
         if offset >= self.day_count {
             return Err(AvailabilityError::DayOutOfRange(day));
         }
-        usize::try_from(offset).map_err(|_| AvailabilityError::SizeOverflow)
+        Ok(offset as usize)
     }
 }
 
diff --git a/crates/veyra-cdc/src/checkpoint.rs b/crates/veyra-cdc/src/checkpoint.rs
index 3b4f184..1528935 100644
--- a/crates/veyra-cdc/src/checkpoint.rs
+++ b/crates/veyra-cdc/src/checkpoint.rs
@@ -105,7 +105,7 @@ impl AppliedCheckpoint {
         let mut reader = OpenOptions::new().read(true).write(true).open(&self.path)?;
         reader.seek(SeekFrom::Start(0))?;
         let file_len = reader.metadata()?.len();
-        let record_len = u64::try_from(RECORD_LEN).map_err(|_| CheckpointError::LengthOverflow)?;
+        let record_len = RECORD_LEN as u64;
         let complete_len = file_len - (file_len % record_len);
         if complete_len != file_len {
             reader.set_len(complete_len)?;
